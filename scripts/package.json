{
  "name": "@forkara/scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "effect-language-service patch",
    "claude-fast-mode-probe": "bun run claude-fast-mode-probe.ts",
    "claude-haiku-thinking-probe": "bun run claude-haiku-thinking-probe.ts",
    "typecheck": "node ../node_modules/@typescript/native/bin/tsc --noEmit --tsBuildInfoFile node_modules/.cache/typescript-native.tsbuildinfo",
    "typecheck:native": "bun run typecheck",
    "test": "vitest run",
    "typecheck:legacy": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.207",
    "@effect/platform-node": "catalog:",
    "@forkara/contracts": "workspace:*",
    "@forkara/shared": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/language-service": "catalog:",
    "@effect/vitest": "catalog:",
    "@types/bun": "catalog:",
    "electron-builder": "26.15.3",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
