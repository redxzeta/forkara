// FILE: apps/web/src/forkEasterEgg.ts
// Purpose: Store the ASCII-art dinner fork for the settings about section easter egg.
// Layer: Presentation Data / Easter Egg

export const FORK_ASCII_ART = `
   || || || ||
   || || || ||
   || || || ||
   \\__||_||__/
      ||||
      ||||
      ||||
      ||||
      ||||
      ||||
`.trim();
